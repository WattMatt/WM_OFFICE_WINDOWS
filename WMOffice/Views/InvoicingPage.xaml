<Page
    x:Name="Root"
    x:Class="WMOffice.Views.InvoicingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WMOffice.Views"
    xmlns:vm="using:WMOffice.ViewModels"
    xmlns:models="using:WMOffice.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="Invoicing" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,24"/>

        <!-- Add New Invoice & Filter -->
        <Grid Grid.Row="1" Margin="0,0,0,24" ColumnSpacing="12" RowSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Orientation="Horizontal" Spacing="12">
                <TextBox Header="Invoice #" Text="{x:Bind ViewModel.NewInvoiceNumber, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="150"/>
                <CalendarDatePicker Header="Date" Date="{x:Bind ViewModel.NewDate, Mode=TwoWay}" Width="150"/> 
                <!-- Note: WinUI DatePicker/CalendarDatePicker binding might require converter for DateTime vs DateTimeOffset, but trying direct binding first -->
                <NumberBox Header="Amount" Value="{x:Bind ViewModel.NewAmount, Mode=TwoWay}" Width="120" SpinButtonPlacementMode="Inline"/>
                <ComboBox Header="Status" SelectedItem="{x:Bind ViewModel.NewStatus, Mode=TwoWay}" ItemsSource="{x:Bind ViewModel.StatusOptions}" Width="120"/>
                <Button Content="Add Invoice" Command="{x:Bind ViewModel.AddInvoiceCommand}" VerticalAlignment="Bottom" Style="{StaticResource AccentButtonStyle}"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Bottom">
                <TextBlock Text="Filter:" VerticalAlignment="Center"/>
                <ComboBox ItemsSource="{x:Bind ViewModel.FilterOptions}" SelectedItem="{x:Bind ViewModel.SelectedFilter, Mode=TwoWay}" Width="120"/>
            </StackPanel>
        </Grid>

        <!-- Invoice List -->
        <ListView Grid.Row="2" ItemsSource="{x:Bind ViewModel.Invoices}" SelectionMode="None">
            <ListView.Header>
                <Grid Padding="12" Background="{ThemeResource LayerFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="0,0,0,1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Invoice Number" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="1" Text="Date" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="2" Text="Amount" FontWeight="SemiBold" TextAlignment="Right"/>
                    <TextBlock Grid.Column="3" Text="Status" FontWeight="SemiBold" Margin="12,0,0,0"/>
                    <TextBlock Grid.Column="4" Text="Action" FontWeight="SemiBold" Width="80" TextAlignment="Center"/>
                </Grid>
            </ListView.Header>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:Invoice">
                    <Grid Padding="12" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="0,0,0,1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <TextBlock Text="{x:Bind InvoiceNumber}" VerticalAlignment="Center"/>
                        <TextBlock Text="{x:Bind Date.ToString('d')}" VerticalAlignment="Center" Grid.Column="1"/>
                        <TextBlock Text="{x:Bind Amount.ToString('C')}" VerticalAlignment="Center" Grid.Column="2" TextAlignment="Right"/>
                        
                        <Border Grid.Column="3" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="12,0,0,0"
                                Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}" CornerRadius="4" Padding="8,2">
                            <TextBlock Text="{x:Bind Status}" Style="{StaticResource CaptionTextBlockStyle}"/>
                        </Border>

                        <Button Grid.Column="4" Content="Delete" 
                                Command="{Binding ElementName=Root, Path=DataContext.DeleteInvoiceCommand}" 
                                CommandParameter="{Binding}" 
                                Width="80" VerticalAlignment="Center"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        
        <ProgressRing IsActive="{x:Bind ViewModel.IsLoading}" Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center" Width="40" Height="40"/>
    </Grid>
</Page>
