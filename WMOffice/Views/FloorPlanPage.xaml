<Page
    x:Name="MyPage"
    x:Class="WMOffice.Views.FloorPlanPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WMOffice.Views"
    xmlns:vm="using:WMOffice.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:WMOffice.Converters"
    mc:Ignorable="d">

    <Page.DataContext>
        <vm:FloorPlanViewModel/>
    </Page.DataContext>

    <Page.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <Grid HorizontalAlignment="Left" VerticalAlignment="Top">
                <!-- Floor Plan Image -->
                <Image x:Name="FloorPlanImage" Source="/Assets/StoreLogo.png" Stretch="None" PointerPressed="FloorPlanImage_PointerPressed"/>

                <!-- Pins Overlay Canvas -->
                <Canvas x:Name="FloorPlanCanvas" IsHitTestVisible="False"/>
            </Grid>
        </ScrollViewer>
    </Grid>
</Page>
