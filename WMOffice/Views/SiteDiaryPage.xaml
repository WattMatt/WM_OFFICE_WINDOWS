<Page x:Class="WMOffice.Views.SiteDiaryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WMOffice.Views"
      xmlns:vm="clr-namespace:WMOffice.ViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Site Diary">

    <Page.DataContext>
        <vm:SiteDiaryViewModel />
    </Page.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Master List -->
        <Border Grid.Column="0" BorderBrush="Gray" BorderThickness="0,0,1,0" Padding="10">
            <DockPanel>
                <TextBlock Text="Daily Logs" DockPanel.Dock="Top" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
                <Button Content="Add New Entry" Command="{Binding AddEntryCommand}" DockPanel.Dock="Bottom" Margin="0,10,0,0" Padding="5"/>
                
                <ListBox ItemsSource="{Binding DiaryEntries}" SelectedItem="{Binding SelectedEntry}" DisplayMemberPath="Date" 
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="0,5">
                                <TextBlock Text="{Binding Date, StringFormat=d}" FontWeight="Bold"/>
                                <TextBlock Text="{Binding WeatherSummary}" FontSize="11" Foreground="Gray" TextTrimming="CharacterEllipsis"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
        </Border>

        <!-- Detail View -->
        <Border Grid.Column="1" Padding="20">
            <Grid>
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectedEntry}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>
                
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,15">
                    <TextBlock Text="Date" FontWeight="SemiBold"/>
                    <DatePicker SelectedDate="{Binding SelectedEntry.Date}"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Margin="0,0,0,15">
                    <TextBlock Text="Weather Summary" FontWeight="SemiBold"/>
                    <TextBox Text="{Binding SelectedEntry.WeatherSummary, UpdateSourceTrigger=PropertyChanged}" Padding="5"/>
                </StackPanel>

                <StackPanel Grid.Row="2" Margin="0,0,0,15">
                    <TextBlock Text="Notes" FontWeight="SemiBold"/>
                    <TextBox Text="{Binding SelectedEntry.Notes, UpdateSourceTrigger=PropertyChanged}" 
                             AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Height="200" Padding="5"/>
                </StackPanel>

                <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Save" Command="{Binding SaveCommand}" Width="80" Margin="0,0,10,0"/>
                    <Button Content="Delete" Command="{Binding DeleteCommand}" Width="80"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- Empty State Message -->
        <TextBlock Grid.Column="1" Text="Select an entry to view details" 
                   HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Gray"
                   Visibility="{Binding SelectedEntry, Converter={StaticResource NullToVisibilityConverter}, ConverterParameter=Inverse}"/> 
                   <!-- Assuming a converter exists or this is just placeholder logic -->

    </Grid>
</Page>
