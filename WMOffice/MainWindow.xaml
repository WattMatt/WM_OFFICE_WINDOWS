<Window
    x:Class="WMOffice.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WMOffice"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:WMOffice.Models"
    mc:Ignorable="d">

    <Grid>
        <NavigationView x:Name="NavView" PaneDisplayMode="Left" SelectionChanged="NavView_SelectionChanged">
            <NavigationView.MenuItems>
                <NavigationViewItem Icon="Home" Content="Dashboard" Tag="Dashboard" />
                <NavigationViewItem Icon="Library" Content="Projects" Tag="Projects" />
            </NavigationView.MenuItems>

            <Frame x:Name="ContentFrame" />
        </NavigationView>

        <!-- Inline definition for simplicity in this scaffold (normally separate pages) -->
        <StackPanel x:Name="DashboardPanel" Margin="24,12,0,0" Visibility="Visible">
            <TextBlock Text="Project Dashboard" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,24"/>
            
            <ListView x:Name="ProjectsList" ItemsSource="{x:Bind Projects}">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:Project">
                        <StackPanel Orientation="Horizontal" Padding="12">
                            <TextBlock Text="{x:Bind Name}" Style="{StaticResource SubtitleTextBlockStyle}" Width="200"/>
                            <TextBlock Text="{x:Bind Status}" Style="{StaticResource BodyTextBlockStyle}" Foreground="Gray"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </Grid>
</Window>
